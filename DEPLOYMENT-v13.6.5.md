# Deployment · Luvia 13.6.5 / Core 4.6.5

## GitHub Pages

```bash
git add .
git commit -m "fix(places): close canonical timeline writes and collection actions"
git push
```

## Supabase

- SQL-Migration: nicht erforderlich
- Edge Function Deployment: nicht erforderlich
- Secrets: keine Änderungen

## PWA

Neuer Cache: `luvia-shell-v13.6.5`

Nach erfolgreichem Pages-Deployment die installierte PWA vollständig schließen und erneut öffnen.
