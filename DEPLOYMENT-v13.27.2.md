# Deployment 13.27.2

```powershell
supabase functions deploy luvia-gateway
supabase functions deploy luvia-intelligence
git add .
git commit -m "fix(places): reconcile core health lifecycle search and unified surface"
git push
```
