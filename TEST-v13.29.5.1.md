# Test 13.29.5.1

- Alte Fotos ohne `thumb_256_path`/`thumb_640_path` werden wieder angezeigt.
- Neue Fotos verwenden weiterhin direkte öffentliche Thumbnails.
- Backfill liefert keine `[object Object]`-Fehler mehr.
- Ein fehlerhaftes Medium beendet nicht den gesamten Durchlauf.
- `node --check` für geänderte JavaScript-Dateien.
