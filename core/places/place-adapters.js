(function(){
'use strict';
const VERSION='4.11.2';
const R=window.LuviaPlaceRegistry,D=window.LuviaPlaceDomain;
function adapterFor(type){return Object.freeze({version:VERSION,type,normalize(raw,context={}){if(raw?.place&&raw?.tripPlace&&window.LuviaPlaceEntities)return window.LuviaPlaceEntities.entityToPlace(raw);return D.normalize(raw,{...context,primaryType:type,source:raw?.source||context.source||'google_places',capabilities:R.getCapabilities(type)});},status(){const provider=type==='cycling_route'?Boolean(window.LuviaCyclingRoutes?.search):Boolean(window.LuviaPlaces?.textSearch),entities=Boolean(window.LuviaPlaceEntities?.list);const productive=['restaurant','accommodation','attraction','photo_spot','shopping','nature','cycling_route'].includes(type);return provider&&entities?{state:productive?'ready':'provider_ready',ready:true,sourceData:true,moduleVisible:productive,reason:type==='restaurant'?'Universeller Place Core und Restaurant-Erweiterung sind verbunden.':type==='accommodation'?'Stay Core und Unterkunftsmodul sind cloudseitig verbunden.':type==='attraction'?'Sehenswürdigkeiten und Aktivitäten verwenden den universellen Place Core.':type==='photo_spot'?'Fotospots und Solar Intelligence sind produktiv verbunden.':type==='shopping'?'Shopping und Shopping Intelligence sind produktiv verbunden.':type==='nature'?'Natur & Ausflüge sowie Nature Intelligence sind produktiv verbunden.':type==='cycling_route'?'Fahrradrouten, MTB-Trails und Cycling Route Intelligence sind produktiv verbunden.':'Provider- und Cloud-Datenquelle sind produktionsfähig; sichtbares Fachmodul folgt.'}:{state:'degraded',ready:false,sourceData:false,reason:'Places Gateway oder Place Entity Service ist nicht geladen.'};},async load(context={}){if(!window.LuviaPlaceEntities?.list)return[];const response=await window.LuviaPlaceEntities.list({tripId:context.tripId,type});return(response?.data?.entities||[]).map(x=>this.normalize(x,context));},search(options={}){return type==='cycling_route'?window.LuviaCyclingRoutes.search(options.query||'',options):window.LuviaPlaceEntities.searchPlaces({...options,type});},import(providerPlaceId,options={}){return window.LuviaPlaceEntities.importPlace(providerPlaceId,{...options,type});}});}
D.TYPES.forEach(type=>R.registerAdapter(type,adapterFor(type)));
window.LuviaPlaceAdapters=Object.freeze({version:VERSION,get:type=>R.getAdapter(type)});
})();
