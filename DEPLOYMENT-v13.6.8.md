# Deployment 13.6.8

Keine SQL-Migration und kein Edge-Function-Deployment erforderlich.

```bash
git add .
git commit -m "refactor(places): enforce one global favorite system"
git push
```

PWA-Cache: `luvia-shell-v13.6.8`
