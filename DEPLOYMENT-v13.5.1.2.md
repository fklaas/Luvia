# Deployment 13.5.1.2

Frontend-only deployment. No SQL migration, Edge Function deployment, or secret changes are required.

After deployment, close and reopen the PWA and run:

```javascript
await LuviaPlaceConformance.runAll()
```
