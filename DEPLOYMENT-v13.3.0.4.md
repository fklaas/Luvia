# Deployment – Luvia 13.3.0.4

Nur das Frontend veröffentlichen.

1. vollständige Projektdateien deployen
2. GitHub-Pages-Build abwarten
3. auf iPhone Safari den Tab schließen und `myluvia.app` erneut öffnen
4. bei alter Darstellung Website-Daten bzw. PWA-Cache aktualisieren

Keine SQL-Migration, kein Functions-Deployment, keine neuen Secrets und keine RLS-Änderung.

Service-Worker-Cache: `luvia-shell-v13.3.0.4`
