# Deployment 13.27.4

Keine Datenbankmigration und kein Function-Deployment erforderlich.

```powershell
git add .
git commit -m "fix(places): align lifecycle with deployed visit schema"
git push
```

Danach `force-update.html` ausführen.
