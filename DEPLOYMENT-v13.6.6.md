# Deployment 13.6.6

Keine SQL-Migration, keine neuen Secrets und kein Edge-Function-Deployment erforderlich.

```bash
git add .
git commit -m "fix(places): restore restaurant registration and codify the global place architecture"
git push
```

Nach GitHub Pages Deployment die PWA vollständig schließen und neu öffnen.

PWA-Cache: `luvia-shell-v13.6.6`
