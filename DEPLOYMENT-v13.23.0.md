# Deployment 13.23.0

Keine Datenbankmigration und kein zwingendes Edge-Function-Deployment. Die Recherche verwendet bestehende `luvia-gateway`- und Place-Entity-Verträge.

```powershell
git add .
git commit -m "feat(planning): add candidate research engine"
git push
```

Danach `force-update.html` ausführen und `13.23.0 · Core 4.23.0` kontrollieren.
