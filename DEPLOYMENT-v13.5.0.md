# Deployment 13.5.0

Keine SQL-Migration, keine Edge-Function-Änderung und keine neuen Secrets erforderlich.

1. Frontend deployen.
2. Service-Worker-Cache `luvia-shell-v13.5.0` aktiv werden lassen.
3. App vollständig schließen und neu öffnen.
4. Developer Console: `await LuviaPlaceConformance.runAll()` ausführen.
