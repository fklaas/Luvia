# Deployment 13.6.9

Keine SQL-Migration und kein Edge-Function-Deployment erforderlich.

```bash
git add .
git commit -m "fix(places): remove restaurant favorite legacy paths"
git push
```

PWA-Cache: `luvia-shell-v13.6.9`
