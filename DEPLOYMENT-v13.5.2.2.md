# Deployment v13.5.2.2

Frontend-only deployment.

```bash
git add .
git commit -m "fix(places): unify favorite UI warm timeline media and preserve viewport"
git push
```

PWA cache: `luvia-shell-v13.5.2.2`

After deployment, fully close and reopen Luvia once.
