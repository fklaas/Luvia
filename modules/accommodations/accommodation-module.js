(()=>{'use strict';
const MODULE_ID='accommodations',VERSION='4.3.2';
const FILTERS=[['🏨','Entdecken','Unterkünfte'],['🏨','Hotel','Hotel'],['🏢','Apartment','Apartment'],['🏡','Ferienhaus','Ferienhaus'],['🛏️','Hostel','Hostel'],['⛺','Camping','Campingplatz'],['🌴','Resort','Resort']];
const state={root:null,trip:null,query:'',filter:'Unterkünfte',results:[],entities:[],loading:false,error:null,searchMap:new Map()};
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const tripId=()=>state.trip?.id||state.trip?.tripId||window.LuviaTripStore?.snapshot?.().activeTrip?.id;
const destination=()=>state.trip?.destination?.name||state.trip?.destinationName||'eurem Reiseziel';
const place=e=>e?.place||e||{},link=e=>e?.tripPlace||e?.trip_place||{},ext=e=>e?.extension||e?.accommodation||{};
const providerId=p=>p?.providerPlaceId||p?.provider_place_id||p?.sourceId||p?.source_id||p?.id||'';
const typeLabel=p=>p?.primaryTypeLabel||p?.primary_type_label||p?.types?.find?.(x=>/hotel|lodging|hostel|camp|resort|apartment/i.test(x))||'Unterkunft';
function img(p){const ph=p?.photos?.[0];return ph?.uri||ph?.url||ph?.photoUri||''}
function normalized(p,l={}){return {...p,id:p.id||p.placeId||providerId(p),placeId:p.id||p.placeId,primaryType:'accommodation',symbol:'🏨',lifecycle:l.status||p.lifecycleStatus||'discovered',address:p.formattedAddress||p.formatted_address||p.address||'',matchScore:p.matchScore||p.match_score||Math.max(50,Math.min(96,Math.round((Number(p.rating)||3.8)*17))),metadata:{...(p.metadata||{}),tripPlaceId:l.id}}}
function card(p,l={},saved=false){p=normalized(p,l);const media=img(p)?`<img class="rv2-card-photo" src="${esc(img(p))}" alt="${esc(p.name)}" loading="lazy">`:`<div class="rv2-card-photo rv2-photo-fallback">🏨</div>`;const actions=saved?`<button class="rv2-action primary" data-open-saved="${esc(l.id)}">✨ Ort öffnen</button><button class="rv2-action" data-favorite="${esc(l.id)}">${l.is_favorite?'♥':'♡'} Favorit</button>`:`<button class="rv2-action primary" data-open-result="${esc(providerId(p))}">✨ Ort öffnen</button><button class="rv2-action" data-import="${esc(providerId(p))}">♡ Merken</button>`;return window.LuviaPlaceUI.card(p,{className:'rv2-card rv2-card-intelligent',media,intelligence:{score:p.matchScore,openLabel:p.openNow===true?'Jetzt geöffnet':p.openNow===false?'Aktuell geschlossen':'Details verfügbar'},actions});}
function render(){if(!state.root)return;const t=state.trip,accent=t?.accent||'#668fd1';state.root.className='luvia-restaurants-v2';state.root.style.setProperty('--trip-accent',accent);state.root.innerHTML=`<div class="rv2-shell"><section class="rv2-hero"><span class="rv2-kicker">Luvia Places · ${esc(destination())}</span><h1>Unterkünfte, die zu eurer Reise passen</h1><p>Entdecken, merken und als Ausgangspunkt in euren Reiseablauf einbauen.</p></section><section class="rv2-discovery"><div class="rv2-discovery-copy"><span class="rv2-eyebrow">Für euch in ${esc(destination())}</span><div class="rv2-discovery-title-row"><div><h2>Wo möchtet ihr wohnen?</h2><p>Direkte Vorschläge aus eurem Reiseziel – im selben Place-Erlebnis wie Restaurants.</p></div><small class="rv2-live-badge">Live empfohlen</small></div><div class="rv2-searchbar"><input data-query value="${esc(state.query)}" placeholder="Hotel, Apartment, Ferienhaus oder Stimmung"><button class="rv2-search-submit" data-search>Suchen</button></div><div class="rv2-quick-label">Direkt entdecken</div><div class="rv2-quick-filters">${FILTERS.map(([i,l,q])=>`<button class="rv2-quick-filter ${state.filter===q?'on':''}" data-filter="${esc(q)}">${i} ${l}</button>`).join('')}</div></div><div class="rv2-recommendation-head"><div><span>Persönliche Auswahl</span><h3>Gerade passend für euch</h3></div><button data-refresh>Aktualisieren</button></div>${state.error?`<div class="lv-error">${esc(state.error)}</div>`:''}<small>${state.results.length} passende Vorschläge</small><div class="rv2-grid">${state.loading?'<div class="stay-empty">Unterkünfte werden gesucht …</div>':state.results.map(p=>card(p)).join('')||'<div class="stay-empty">Starte eine Suche oder wähle eine Kategorie.</div>'}</div></section><section class="rv2-library"><div class="rv2-library-heading"><div><span>Eure Sammlung</span><h2>Gemerkte Unterkünfte</h2></div></div><div class="rv2-grid">${state.entities.map(e=>card(place(e),link(e),true)).join('')||'<div class="stay-empty">Hier erscheinen eure gemerkten Unterkünfte.</div>'}</div></section></div>`;bind()}
async function load(){try{const r=await window.LuviaPlaceEntities.list({tripId:tripId(),type:'accommodation'});state.entities=(r?.data?.entities||[]).filter(e=>link(e).status!=='archived')}catch(e){state.error=e.message||String(e)}render()}
async function search(){state.query=state.root.querySelector('[data-query]')?.value?.trim()||state.query;state.loading=true;state.error=null;render();try{const q=[state.query,state.filter,destination()].filter(Boolean).join(' ');const r=await window.LuviaPlaceEntities.searchPlaces({tripId:tripId(),type:'accommodation',query:q,maxResultCount:16,strictDestination:true});state.results=r?.data?.places||[];state.searchMap=new Map(state.results.map(p=>[String(providerId(p)),p]))}catch(e){state.error=e.message||String(e)}state.loading=false;render()}
async function importOne(id){try{await window.LuviaPlaceEntities.importPlace(id,{tripId:tripId(),type:'accommodation',tripPlace:{status:'saved',isFavorite:true},extension:{accommodationType:state.filter.toLowerCase()}});await load();window.LuviaUIKit?.toast?.('Unterkunft gemerkt.',{type:'success'})}catch(e){state.error=e.message||String(e);render()}}
function facts(p){return `<div class="rv2-facts"><span>⭐ ${p.rating?Number(p.rating).toFixed(1).replace('.',','):'–'} <small>${p.userRatingCount||p.user_rating_count||0} Bewertungen</small></span><span>📍 ${esc(p.formattedAddress||p.address||destination())}</span><span>${p.openNow===true?'🟢 Jetzt geöffnet':'🕒 Details verfügbar'}</span></div>`}
async function detail(p,l=null,e=null){p=normalized(p,l||{});let photos=[];try{const d=await window.LuviaPlaces?.details?.(providerId(p),{regionCode:'DE'});p={...p,...(d?.data?.place||{})};photos=(p.photos||[]).slice(0,5)}catch(_){}const back=document.createElement('div');back.className='rv2-experience-backdrop';back.innerHTML=`<article class="rv2-experience" role="dialog"><button class="rv2-experience-close" data-close>×</button><div class="rv2-hero-gallery ${photos.length?'':'empty'}">${photos.length?photos.map((x,i)=>`<div class="rv2-gallery-photo ${i===0?'primary':''}"><img src="${esc(x.uri||x.url||'')}" alt="${esc(p.name)}"></div>`).join(''):`<div class="rv2-gallery-fallback">🏨<span>${esc(p.name)}</span></div>`}</div><div class="rv2-experience-body"><div class="rv2-experience-title"><div><span class="rv2-detail-kicker">${esc(typeLabel(p))} · ${esc(destination())}</span><h2>${esc(p.name)}</h2><p>📍 ${esc(p.formattedAddress||p.address||destination())}</p></div><div class="rv2-match"><strong>${p.matchScore||75} %</strong><span>Passt zu euch</span></div></div><div class="rv2-detail-primary-actions">${l?`<button data-fav>${l.is_favorite?'♥':'♡'} Favorit</button><button data-booked>✓ Als gebucht</button><button data-edit>🏨 Aufenthalt</button>`:`<button data-import-detail>♡ Merken</button>`}</div>${facts(p)}${window.LuviaPlaceUI?.assessment?.({score:p.matchScore,reasons:['Liegt am aktiven Reiseziel','Kann als Ausgangspunkt für Wege und Tagesplanung dienen','Wird mit Timeline und Today verbunden']})||''}<section class="rv2-intelligence-grid"><article><span>Reisebasis</span><strong>${ext(e).is_trip_base?'Fester Ausgangspunkt':'Noch nicht festgelegt'}</strong><small>Beeinflusst Wege, Abfahrten und Empfehlungen.</small></article><article><span>Aufenthalt</span><strong>${ext(e).check_in_at?'Check-in erfasst':'Noch offen'}</strong><small>Check-in und Check-out erscheinen in Timeline und Today.</small></article><article><span>Persönlicher Match</span><strong>${p.matchScore||75} %</strong><small>Basierend auf Ziel, Lage und Reisekontext.</small></article></section>${l?editForm(e):''}</div></article>`;document.body.appendChild(back);document.body.style.overflow='hidden';const close=()=>{back.remove();document.body.style.overflow=''};back.onclick=x=>{if(x.target===back||x.target.closest('[data-close]'))close()};back.querySelector('[data-import-detail]')?.addEventListener('click',async()=>{await importOne(providerId(p));close()});back.querySelector('[data-fav]')?.addEventListener('click',async()=>{await window.LuviaPlaceEntities.updateLifecycle(l.id,l.status||'saved',{isFavorite:!l.is_favorite},{tripId:tripId()});close();load()});back.querySelector('[data-booked]')?.addEventListener('click',async()=>{await window.LuviaPlaceEntities.updateLifecycle(l.id,'booked',{}, {tripId:tripId()});close();load()});back.querySelector('[data-save-stay]')?.addEventListener('click',async()=>saveStay(back,e,close))}
function editForm(e){const x=ext(e);return `<section class="rv2-workspace-section"><span class="rv2-workspace-kicker">Unterkunft & Buchung</span><div class="stay-form"><label>Check-in<input type="datetime-local" data-in value="${x.check_in_at?new Date(x.check_in_at).toISOString().slice(0,16):''}"></label><label>Check-out<input type="datetime-local" data-out value="${x.check_out_at?new Date(x.check_out_at).toISOString().slice(0,16):''}"></label><label>Gäste<input type="number" min="1" data-guests value="${x.guest_count||1}"></label><label>Zimmer<input type="number" min="1" data-rooms value="${x.room_count||1}"></label><label>Buchungsnummer<input data-booking value="${esc(x.booking_number||'')}"></label><label>Anbieter<input data-provider value="${esc(x.booking_provider||'')}"></label><label class="wide check"><input type="checkbox" data-base ${x.is_trip_base?'checked':''}> Fester Ausgangspunkt</label><label class="wide">Notizen<textarea data-notes>${esc(x.notes||'')}</textarea></label></div><button class="rv2-action primary" data-save-stay>Aufenthalt speichern</button></section>`}
async function saveStay(root,e,close){const l=link(e),p=place(e),accommodation={checkInAt:root.querySelector('[data-in]').value||null,checkOutAt:root.querySelector('[data-out]').value||null,guestCount:Number(root.querySelector('[data-guests]').value||1),roomCount:Number(root.querySelector('[data-rooms]').value||1),bookingNumber:root.querySelector('[data-booking]').value.trim()||null,bookingProvider:root.querySelector('[data-provider]').value.trim()||null,isTripBase:root.querySelector('[data-base]').checked,notes:root.querySelector('[data-notes]').value.trim()};await window.LuviaPlaceEntities.updateAccommodation({tripId:tripId(),tripPlaceId:l.id,status:l.status||'saved',accommodation});for(const [kind,value] of [['in',accommodation.checkInAt],['out',accommodation.checkOutAt]])if(value)await window.LuviaScheduleIntelligence?.upsertEvent?.({tripId:tripId(),entityType:'accommodation',title:`${kind==='in'?'Check-in':'Check-out'} · ${p.name}`,date:value.slice(0,10),time:value.slice(11,16),durationMinutes:30,placeId:p.id,tripPlaceId:l.id,sourceKey:`accommodation:${l.id}:${kind}`});await window.LuviaTimelineCore?.record?.({tripId:tripId(),placeId:p.id,type:'accommodation.updated',title:`Unterkunft aktualisiert: ${p.name}`,source:'accommodation',automatic:true,metadata:{tripPlaceId:l.id}});close();load()}
function bind(){state.root.querySelector('[data-search]')?.addEventListener('click',search);state.root.querySelector('[data-query]')?.addEventListener('keydown',e=>e.key==='Enter'&&search());state.root.querySelector('[data-refresh]')?.addEventListener('click',search);state.root.querySelectorAll('[data-filter]').forEach(b=>b.onclick=()=>{state.filter=b.dataset.filter;search()});state.root.querySelectorAll('[data-import]').forEach(b=>b.onclick=()=>importOne(b.dataset.import));state.root.querySelectorAll('[data-open-result]').forEach(b=>b.onclick=()=>{const p=state.searchMap.get(String(b.dataset.openResult));if(p)detail(p)});state.root.querySelectorAll('[data-open-saved]').forEach(b=>b.onclick=()=>{const e=state.entities.find(x=>String(link(x).id)===String(b.dataset.openSaved));if(e)detail(place(e),link(e),e)});state.root.querySelectorAll('[data-favorite]').forEach(b=>b.onclick=async()=>{const e=state.entities.find(x=>String(link(x).id)===String(b.dataset.favorite));await window.LuviaPlaceEntities.updateLifecycle(link(e).id,link(e).status||'saved',{isFavorite:!link(e).is_favorite},{tripId:tripId()});load()})}
async function mount(instance,context={}){state.root=instance?.roots?.[0]||document.querySelector('#accommodations-module');state.trip=context.trip||instance?.trip||window.LuviaTripStore?.snapshot?.().activeTrip;render();await load();if(!state.results.length)await search();return instance}
function unmount(){state.root=null;state.trip=null}
async function openPlace(payload={}){const e=state.entities.find(x=>String(link(x).id)===String(payload.tripPlaceId)||String(place(x).id)===String(payload.placeId));if(e){detail(place(e),link(e),e);return true}return false}
window.addEventListener('luvia:open-place',e=>{if(e.detail?.type==='accommodation')openPlace(e.detail)});
window.LuviaAccommodations=Object.freeze({version:VERSION,load,search,openPlace});
window.LuviaModules?.register({id:MODULE_ID,order:60,group:'Places',icon:'🏨',title:'Unterkünfte',description:'Unterkünfte im universellen Places-Erlebnis.',selectors:['#accommodations-module'],version:VERSION,mount,unmount});
})();
