# Deployment 13.5.1.4

Nur Frontend deployen. Keine Backendänderungen erforderlich.

```bash
git add .
git commit -m "fix(places): finalize provider labels and global detail contacts"
git push
```

Nach dem Deployment die PWA vollständig schließen und neu öffnen.
