# Deployment 13.1.3.9

Keine Migration, keine Edge-Function-Änderung und keine neuen Secrets.

1. Projektdateien übernehmen.
2. `git add .`
3. `git commit -m "fix(restaurants): sync suggestions and add schedule reset"`
4. `git push`
5. Hosting-Build abwarten und PWA aktualisieren.
