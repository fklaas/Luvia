# Deployment 13.28.2.2

Nur Frontend. Kein `supabase db push`, keine Edge Function, keine Secrets.

```bash
git add .
git commit -m "fix: make memory evidence understandable and render photo memories in timeline"
git push
```

Danach `force-update.html` ausführen und App 13.28.2.2 / Core 4.28.2.2 prüfen.
