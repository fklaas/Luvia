# Deployment – Luvia 13.27.1

```powershell
git add .
git commit -m "fix(places): restore sequential wishes and realtime lifecycle"
git push
```

Danach PWA schließen, `force-update.html` öffnen und auf `13.27.1 · Core 4.27.1` prüfen. Keine Supabase Function und keine Migration erforderlich.
