# Deployment 13.1.0.1

No Supabase migration, Edge Function deployment, secret or environment change is required.

```bash
git add .
git commit -m "fix(today): open places and smooth realtime updates"
git push
```

After hosting deployment, refresh the PWA/service-worker cache.
