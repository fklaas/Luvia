# Luvia 13.20.1 – Reliable Conversational Results

- Eliminates oversized discovery payloads and redundant AI planning requests.
- Uses deterministic domain-first intent parsing before provider search.
- Enforces explicit vegetarian/vegan matching.
- Adds a soft search journey and swipeable curated choice carousel.
- Keeps results inside Places instead of falling back to Dashboard.
- Move remains transport-only.
