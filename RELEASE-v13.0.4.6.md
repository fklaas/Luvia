# Luvia v13.0.4.6 — Diagnostics Context-Aware Health Fix

Core 4.0.4.6 / Build 13.0.4.6.

- Core-4 smoke tests use the lightweight service registry list instead of deep diagnostics.
- Restaurant adapter health is resolved through the registry status contract.
- Auth, user and PWA tests understand the intentionally isolated diagnostics runtime.
- Remote-session-dependent checks are reported as controlled skips rather than product failures.
