# Deployment 13.0.4.3

Keine Migration, keine Edge-Function und keine neuen Secrets erforderlich.

```bash
git add .
git commit -m "fix(core): break diagnostics recursion and stabilize schedule removal"
git push
```

Anschließend PWA-/Browser-Cache aktualisieren.
