# Deployment v13.0.4.5

Keine Migration und kein Edge-Function-Deployment erforderlich.

```bash
git add .
git commit -m "fix(diagnostics): boot developer tools without remote services"
git push
```

Danach Service Worker und Cache aktualisieren.
