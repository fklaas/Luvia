# Deployment 13.4.3

1. `supabase db push` im Projektverzeichnis ausführen. Migration 033 erstellt `trip_place_data` und aktiviert RLS/Realtime.
2. Kein Gateway-Deployment und keine neuen Secrets erforderlich.
3. Frontend deployen.
4. PWA vollständig schließen und neu öffnen. Cache: `luvia-shell-v13.4.3`.
