# Deployment 13.6.4

No SQL migration, Edge Function deployment or new secret is required.

```bash
git add .
git commit -m "fix(places): enforce the canonical module shell for attractions"
git push
```

After GitHub Pages has deployed, close the installed PWA completely and reopen it once so that `luvia-shell-v13.6.4` becomes active.
