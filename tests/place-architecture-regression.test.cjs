/* Build 13.11.2 – static architecture regression test */
const fs=require('fs');
const assert=require('assert');
const files=['modules/restaurants-v2/restaurant-module.js','modules/accommodations/accommodation-module.js','modules/attractions/attraction-module.js','modules/photo-spots/photo-spot-module.js','modules/shopping/shopping-module.js','modules/nature/nature-module.js','modules/cycling-routes/cycling-route-module.js'];
for(const file of files){const text=fs.readFileSync(file,'utf8');assert(text.includes('LuviaPlaceExperience.discovery'),`${file}: global discovery shell missing`);assert(text.includes('LuviaPlaceCollections.favoritePanel'),`${file}: global favorite panel missing`);assert(text.includes('LuviaPlaceUI.card'),`${file}: global card missing`);assert(!/function\s+(setFavorite|toggleFavorite|clearFavorites)\s*\(/.test(text),`${file}: local favorite writer forbidden`);assert(!/LuviaPlaceEntities\.updateLifecycle\([^)]*isFavorite/.test(text),`${file}: direct favorite lifecycle write forbidden`);}
const index=fs.readFileSync('index.html','utf8');assert(index.includes('place-runtime-store.js'));assert(index.includes('place-command-service.js'));const placeUi=fs.readFileSync('core/places/place-ui.js','utf8');assert(index.includes('shopping-intelligence-service.js'));assert(index.includes('modules/shopping/shopping-module.js'));assert(index.includes('nature-intelligence-service.js'));assert(index.includes('modules/nature/nature-module.js'));assert(index.includes('cycling-route-intelligence-service.js'));assert(index.includes('modules/cycling-routes/cycling-route-module.js'));assert(placeUi.includes('function insightGrid'),'global insight renderer missing');
console.log('Place architecture static regression: OK');
