const fs=require('fs'),assert=require('assert');const s=fs.readFileSync('core/context/journey-knowledge-graph.js','utf8');for(const n of ['trip_schedule_events','timeline_events','trip_places','trip_place_data','places','restaurants','reservation','evidenceRefs','luvia:journey-context-changed'])assert(s.includes(n),`missing ${n}`);assert(s.includes("cloudAuthoritative:true"));console.log('Journey Knowledge Graph cloud resolution and invalidation contracts: OK');
