# Deployment 13.25.0

Keine Datenbankmigration und kein Supabase-Function-Deployment erforderlich.

```powershell
git add .
git commit -m "feat(navigation): reset product focus and add lifecycle hubs"
git push
```

Danach `force-update.html` öffnen und die PWA vollständig neu starten.
