# Deployment · Build 13.0.2

1. Open a terminal in the project root containing `index.html` and `supabase/`.
2. Run `supabase db push`.
3. No Edge Function changed and no new secret is required.
4. Commit and push the complete project.
5. Wait for hosting deployment.
6. Clear the old PWA cache or accept the app update.
